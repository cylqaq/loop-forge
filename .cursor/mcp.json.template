{
  "mcpServers": {
    "github-readonly": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub 只读：Issues/PR/Actions。写操作须人工审批。见 docs/ops/mcp-github-triage.md"
    }
  }
}
